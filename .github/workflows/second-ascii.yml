name: Generate ASCII 

on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Multiple steps in single command
          run: |
            chmod +x ascii-script.sh
            ./ascii-script.sh
